import React, { Component } from 'react'
import { connect } from 'react-redux'

import OutputView from '../components/OutputView'

class OutputViewContainer extends Component {
  render() {
    return (
      <OutputView/>
    )
  }
}

export default OutputViewContainer
